sap.ui.define([
    'sap/ui/base/Object'
], function (BaseObject) {
    'use strict';

    const StorageHelper = BaseObject.extend('geosort.util.StorageHelper', {});

    /** TODO: Implement storage of user credentials/tokens in Firebase Storage */

    return StorageHelper;
});
